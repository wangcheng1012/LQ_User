<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:id="@+id/ll_details_item"
              style="@style/mw.hor"
              android:background="@drawable/bg_gray_0rectangle"
              android:gravity="center"
              android:paddingBottom="15dp"
              android:paddingLeft="10dp"
              android:paddingRight="10dp"
              android:paddingTop="15dp">

    <TextView
        android:id="@+id/tv_details_item_min"
        style="@style/ww"
        android:text="00:00~"
        android:textColor="@color/bg_gray"/>

    <TextView
        android:id="@+id/tv_details_item_max"
        style="@style/ww"
        android:text="00:00"
        android:textColor="@color/bg_gray"/>

</LinearLayout>